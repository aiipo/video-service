export interface EventTypesInterface {
  [type: string]: string;
  Escape: 'Escape';
  keydown: 'keydown';
}

export const eventTypes: EventTypesInterface = {
  Escape: 'Escape',
  keydown: 'keydown',
};
